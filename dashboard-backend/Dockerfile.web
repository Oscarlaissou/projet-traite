FROM nginx:alpine

# Copier la configuration nginx spécifique pour PHP
COPY docker/nginx-php.conf /etc/nginx/conf.d/default.conf

# Créer un répertoire pour les fichiers de l'application
RUN mkdir -p /var/www/html

WORKDIR /var/www/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]